#! /usr/bin/env zsh

# THIS SCRIPT MUST BE RUN FROM THE PROJECT ROOT DIRECTORY.


echo
echo "################################"
date
echo "################################"
echo


echo "################################"
echo "The first SANITY CHECK React build has been disabled to speed things up."
echo "The React build which occurs at the start of the NGINX Dockerfile build is all that is needed."
echo


#pushd nucleus
#
#npm run build
#
#popd

